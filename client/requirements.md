## Packages
framer-motion | Smooth animations for page transitions and elements
three | For the 3D grid distortion background
@react-three/fiber | React renderer for Three.js
@react-three/drei | Helpers for React Three Fiber (needed for effects)
date-fns | Formatting event dates

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
